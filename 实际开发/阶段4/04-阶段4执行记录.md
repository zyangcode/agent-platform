# 阶段 4 执行记录

> 本文记录阶段 4 Agent Team 的实际推进状态、提交点、验证命令和当前边界。设计边界见 `01-阶段4AgentTeam设计说明.md`，接口细节见 `02-阶段4接口设计.md`，开发顺序见 `03-阶段4执行顺序.md`。

## 1. 当前状态

截至 2026-05-29，阶段 4 已完成 Step 1-12，代码级收口完成，当前可进入浏览器手工验收：

| Step | 状态 | 说明 |
|---|---|---|
| Step 1 | 已完成 | Profile 增加 `executionMode=BASIC/TEAM`，前后端表单闭环。 |
| Step 2 | 已完成 | Team TaskPlan / ExecutionResult / ReviewResult DTO 与 TaskPlanValidator。 |
| Step 3 | 已完成 | AgentTool 统一工具视图，Resolver / Dispatcher 骨架。 |
| Step 4 | 已完成 | Planner 通过模型生成 TaskPlan，失败重试一次后降级为通用 `MODEL_TASK`。 |
| Step 5 | 已完成 | Executor 执行 `TOOL_TASK` / `MODEL_TASK`，并提供依赖排序器。 |
| Step 6 | 已完成 | Reviewer 审查 TaskPlan、执行结果和 answerDraft，输出 ReviewResult。 |
| Step 7 | 已完成 | TeamRuntimeEventDTO、TeamEventSink、NoopTeamEventSink 事件模型。 |
| Step 8 | 已完成 | Team Runtime 串起 Planner -> Executor -> answerDraft -> Reviewer -> Final。 |
| Step 9 | 已完成 | Gateway/Web SSE 实时输出 `team_*` 事件。 |
| Step 10 | 已完成 | 前端 Chat 右侧展示 Team run 进度、任务、Review 和 Mermaid 文本。 |
| Step 11 | 已完成 | Trace Detail 支持 Team span 友好名称、关键属性和长字段换行。 |
| Step 12 | 已完成 | 阶段 4 手工验收清单、问题台账收口和验收总结。 |

## 2. 已形成的提交点

| 提交 | 内容 |
|---|---|
| `c786382 feat(profile): expose agent execution mode` | Step 1：Profile executionMode 接口与前端配置。 |
| `8b5df5c feat(team): add task plan model and validation` | Step 2：Team DTO 与 TaskPlanValidator 初版。 |
| `9150cd2 fix(team): harden task plan validation dto` | Step 2 加固：null plan、DTO 防御性拷贝、错误定位。 |
| `ec6ede5 docs(stage4): refine agent tool design` | Step 3 前：收敛 AgentTool 设计。 |
| `9b3edbd feat(agent): add unified runtime tool view` | Step 3：AgentTool Resolver / Dispatcher。 |
| `81c3b83 fix(agent): harden tool resolver and team contracts` | Step 3 加固：Resolver null-safe、事件契约和文档漂移修正。 |
| `0aa69ce test(team): enforce task plan tool rules` | Validator 加固：description 必填、MODEL_TASK 禁止 suggestedTool。 |
| `3107290 feat(team): implement planner with fallback plan` | Step 4：Planner 模型调用、JSON 解析、修正重试和 fallback。 |
| `dc60463 feat(team): execute team tasks with authorized tools` | Step 5：Executor 执行工具任务/模型任务，TaskDependencySorter 依赖排序。 |
| `9621780 feat(team): add reviewer and review validation` | Step 6：Reviewer 模型审查、ReviewResultValidator 校验和 fallback warning。 |
| `8268215 feat(team): add runtime event model` | Step 7：Team runtime event DTO、事件 sink 接口和默认 Noop sink。 |
| `b9e2855 feat(team): orchestrate planner executor reviewer runtime` | Step 8：Team Runtime 编排、TeamRunLimiter、Trace / Token 记录。 |
| `a31aee3 feat(chat): stream team runtime events` | Step 9：Gateway/Web SSE 实时输出 Team 事件。 |
| `d68ba2c feat(frontend): show team run progress in chat` | Step 10：Chat Team 运行面板和 Mermaid 文本展示。 |
| `08f3ff0 feat(frontend): render team spans in trace detail` | Step 11：Trace Detail Team span 展示增强。 |
| `8821efe docs(stage4): add team acceptance documents` | Step 12：阶段 4 手工验收清单、问题台账和验收总结。 |
| 本次审查修复 | 阶段 4 复查：Reviewer 重试场景下 Team run 总 usage 汇总补齐所有 Review 调用。 |

## 3. Step 4 复查结论

已复查 `DefaultTeamPlanner`、`PlanTeamCommand`、`TeamPlanner`、`TaskPlanValidator` 与相关测试：

- Planner 只负责拆任务，不调用 Skill/MCP，不生成最终答案。
- Planner 复用现有 `ModelInvokeService`，后续 Token / Trace 链路可以继续沿用模型调用层。
- Planner prompt 展示工具来源，如 `[SKILL] weather`、`[MCP] read_file`。
- 模型输出会先解析成 `TaskPlanDTO`，再交给 `TaskPlanValidator` 校验。
- 非法 JSON、非法计划、空响应、模型调用异常都会重试一次；二次失败后降级为通用单任务 `MODEL_TASK`。
- fallback 未硬编码 weather/search/calculator，普通问题仍能进入后续 Executor 模型任务。
- `TaskPlanValidator` 已注册为 Spring Bean，避免应用上下文启动时缺 Bean。
- Planner 结果通过 `TeamPlanResultDTO` 携带模型调用结果，后续 Orchestrator 可记录 Planner 阶段 token usage。
- retry prompt 会截断上一次错误原因，避免长异常或垃圾输出浪费上下文。

## 4. 最近验证记录

Step 4 提交前已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=DefaultTeamPlannerTest" test
mvn.cmd -s .mvn/settings.xml -pl agent-platform-gateway -am "-Dtest=GatewayApplicationTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
mvn.cmd -s .mvn/settings.xml test
```

结果：上述命令当时均通过。

注意：全量测试执行时，工作区已有与阶段 4 无关的 `core.model` 未提交改动；阶段 4 Step 4 提交只包含 Planner 相关文件和问题台账。

审查修复后已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=DefaultTeamPlannerTest,TaskPlanValidatorTest" test
mvn.cmd -s .mvn/settings.xml -pl agent-platform-gateway -am "-Dtest=GatewayApplicationTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
mvn.cmd -s .mvn/settings.xml test
```

结果：上述命令通过。

## 5. 当前非阻塞项

- 单 Agent / Team final answer 的 token 级逐字流式输出仍暂缓；阶段 4 已完成 Team 事件级实时 SSE。
- Mermaid 当前只展示文本，图形渲染暂缓。
- Team 数据表和历史 Team Run 列表暂缓，运行数据第一版仍通过 Trace 展示。
- 并发 Team 执行和复杂工作流编排暂缓。
- 高危工具二次确认暂缓。
- Jar Skill 上传 / 热加载 UI 暂缓。
- Admin Security / Users 完整管理页暂缓。
- 前端 build 仍有 Vite chunk size warning，属于体积提醒，不阻塞 Demo。

## 6. Step 5 复查结论

已复查 `DefaultTeamExecutor`、`ExecuteTeamTaskCommand`、`TeamTaskExecutionResultDTO` 与 `TaskDependencySorter`：

- Executor 执行单个任务，不负责完整 Team 编排。
- `TaskDependencySorter` 负责把 TaskPlan 按 dependsOn 排序，并拒绝缺失依赖和循环依赖。
- `TOOL_TASK` 调用 `AgentToolDispatcher` 前先校验授权 AgentTool 列表；未授权直接 failed，不调用 Dispatcher。
- `TOOL_TASK` 调用失败只返回 failed，不抛出中断整个 Team。
- `MODEL_TASK` 走 `ModelInvokeService`，prompt 包含用户原始请求、Profile Prompt、当前任务和已完成任务结果。
- Executor 不直接写 Trace / Token；模型调用和工具调用结果会返回给后续 Orchestrator 统一记录。

Step 5 已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=DefaultTeamExecutorTest,TaskDependencySorterTest" test
```

结果：上述命令通过。

## 7. Step 6 复查结论

已复查 `DefaultTeamReviewer`、`ReviewTeamCommand`、`TeamReviewResultDTO` 与 `ReviewResultValidator`：

- Reviewer 只审查 TaskPlan、ExecutionResult 和 answerDraft，不调用 Skill/MCP，不重新规划任务。
- Reviewer 输出 `ReviewResultDTO`，并通过 `TeamReviewResultDTO.modelInvocations` 带回模型调用结果，供后续 Orchestrator 统一记录 Trace / Token。
- Reviewer JSON 非法、校验失败或模型异常时会重试一次；二次失败后 fallback 为 `passed=true` 且带 WARN issue。
- `ReviewResultValidator` 校验 `passed`、issue level、issue taskId、retryTasks 是否引用已有任务。
- answerDraft 为空时 fallback 会追加 `answerDraft is blank` warning。

Step 6 已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=DefaultTeamReviewerTest,ReviewResultValidatorTest" test
```

结果：上述命令通过。

## 8. Step 7 复查结论

已复查 `TeamRuntimeEventDTO`、`TeamEventSink` 与 `NoopTeamEventSink`：

- `TeamRuntimeEventDTO` 覆盖 `team_start`、`team_plan`、`team_task_start`、`team_tool_call`、`team_tool_result`、`team_task_result`、`team_review`、`team_retry`、`team_final`。
- 事件字段保持轻量稳定：`type`、`traceId`、`step`、`taskId`、`toolName`、`status`、`message`、`payload`、`createdAt`。
- `payload` 使用 `JsonNode.deepCopy()` 做防御性拷贝，避免事件创建后被外部修改。
- `TeamEventSink` 只定义 `emit(TeamRuntimeEventDTO)`，Core 不感知 `OutputStream`、`SseEmitter` 或 HTTP/SSE 细节。
- `NoopTeamEventSink` 作为默认空实现，非流式路径或单元测试可安全注入。

Step 7 已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=TeamRuntimeEventDTOTest,NoopTeamEventSinkTest" test
```

结果：上述命令通过。

## 9. 当时下一步

按 `03-阶段4执行顺序.md` 进入 Step 8：实现 Team Orchestrator，串起 Planner -> Executor -> answerDraft -> Reviewer -> Final，并在执行过程中通过 `TeamEventSink` 推送事件。

## 10. Step 8 复查结论

已实现 `TeamRuntimeService` / `DefaultTeamRuntimeService`，串起 Planner -> Executor -> answerDraft -> Reviewer -> Retry -> Final。

- `DefaultTeamRuntimeService` 专注 Team 编排、事件、Trace / Token 记录，并返回 `AgentRunResult`；conversation message / memory 仍由 `DefaultAgentRuntimeService` 统一保存，避免 `core.team` 越界访问 `core.agent.entity`。
- Planner / Executor / Reviewer 的 `ModelInvokeResult.usage()` 统一汇总为 Team run usage，并逐条调用 `TokenUsageService.record(...)`。
- Team event 顺序覆盖 `team_start` -> `team_plan` -> `team_task_start` -> (`team_tool_call` -> `team_tool_result`) -> `team_task_result` -> `team_review` -> (`team_retry`) -> `team_final`。
- `TeamRunLimiter` 提供 maxTasks / maxRetries / maxToolCalls / maxModelCalls / timeoutMs 限制。
- `DefaultAgentRuntimeService` 已根据 `AgentContextDTO.profile.executionMode` 分发：`BASIC` 继续原单 Agent 链路，`TEAM` 委托 `TeamRuntimeService`。
- `AgentRuntimeService.run(command, TeamEventSink)` 和 `TeamRuntimeService.run(command, TeamEventSink)` 已留出请求级 event sink 入口，供 Step 9 Gateway SSE 适配使用。

Step 8 已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=ArchitectureRulesTest,DefaultAgentRuntimeServiceTest,TeamRunLimiterTest,DefaultTeamRuntimeServiceTest,DefaultTeamPlannerTest,DefaultTeamExecutorTest,DefaultTeamReviewerTest,TeamRuntimeEventDTOTest,NoopTeamEventSinkTest" test
mvn.cmd -s .mvn/settings.xml test
```

结果：阶段 4 核心 43 个用例通过；全量 Maven Reactor 5 个模块通过。

## 11. 当时下一步

按 `03-阶段4执行顺序.md` 进入 Step 9：Gateway/Web SSE 输出 Team 事件。Gateway 需要使用请求级 `TeamEventSink` 把 Core `TeamRuntimeEventDTO` 转成 SSE payload，并保持边执行边写出。

## 12. Step 9 复查结论

已实现 Gateway/Web SSE 输出 Team 事件：

- `InternalAiController` 调用 `AgentRuntimeService.run(command, TeamEventSink)`，用请求级 `TeamEventSink` 将 Core `TeamRuntimeEventDTO` 立即写成 SSE。
- Team SSE 使用独立 `TeamSseEventPayload`，`role`、`taskId`、`toolName`、`status`、`payload` 放在顶层字段，避免 Step 10 前端 Team 面板再从 `metadata` 二次解析。
- Web `ChatController` 保持原始字节透传，不合并、不改写 Gateway 返回的 `team_*` 事件。
- 前端 `ChatStreamEvent` 已补充 `team_start`、`team_plan`、`team_task_start`、`team_tool_call`、`team_tool_result`、`team_task_result`、`team_review`、`team_retry`、`team_final` 类型。

Step 9 已执行：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-gateway -am "-Dtest=InternalAiControllerTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
mvn.cmd -s .mvn/settings.xml -pl agent-platform-web -am "-Dtest=ChatControllerTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
npm --prefix agent-platform-frontend run test -- parse-sse
npm --prefix agent-platform-frontend run build
mvn.cmd -s .mvn/settings.xml test
```

结果：上述命令通过。前端 build 仍有已知 Vite chunk size warning：`dist/assets/index-*.js` 超过 500 kB，不影响构建产物生成。

## 13. Step 10 复查结论

已实现前端 Profile 和 Chat Team 面板收口：

- Profile 创建、编辑、详情页已经支持 `executionMode=BASIC/TEAM`，Step 10 不重复改 Profile 表单。
- Chat 右侧 `RuntimeDetailPanel` 增加 `TeamRunPanel`，仅当收到 `team_*` 事件时显示，不影响 BASIC / Direct model 模式。
- `buildTeamRunSummary` 会从 `team_plan`、`team_task_start`、`team_tool_call`、`team_tool_result`、`team_task_result`、`team_review` 汇总目标、任务状态、工具名、审查结果和 Mermaid 文本。
- Mermaid 第一版按文档要求只读展示，不引入渲染库，避免阶段 4 临近收口时扩大前端依赖和交互范围。
- Team 面板文案已接入现有中英文切换，不新增硬编码语言断点。

Step 10 已执行：

```powershell
npm --prefix agent-platform-frontend run test -- team-run-utils parse-sse
npm --prefix agent-platform-frontend run build
```

结果：上述命令通过。前端 build 仍有已知 Vite chunk size warning：`dist/assets/index-*.js` 超过 500 kB，不影响构建产物生成。

## 14. 当时下一步

按 `03-阶段4执行顺序.md` 进入 Step 11：Trace Detail 支持 Team Span。重点是给 Team span 增加中文名称映射、展示 role/taskId/toolName/retryIndex 等属性，并修复长字段重叠或显示不完整问题。

## 15. Step 11 复查结论

已实现 Trace Detail 对 Team Span 的前端展示增强：

- 新增 `trace-labels.ts`，统一处理 Team span 名称映射和结构化属性提取。
- `TraceTimeline` 会把 `team.run`、`team.plan`、`team.task.execute`、`team.review` 显示为中英文友好的名称，并在时间线卡片中展示前几个关键属性。
- `SpanDetailPanel` 会展示 Team span 的 role、taskId、taskType、toolName、retryIndex、profileId 等结构化字段；当前后端尚未写入的字段不会显示空占位。
- Attributes 原始 JSON 保留，但改为容器内换行，避免长字段造成重叠或“看起来没显示完整”。

Step 11 已执行：

```powershell
npm --prefix agent-platform-frontend run test -- trace-labels trace-utils
npm --prefix agent-platform-frontend run build
```

结果：上述命令通过。前端 build 仍有已知 Vite chunk size warning：`dist/assets/index-*.js` 超过 500 kB，不影响构建产物生成。

## 16. 当时下一步

按 `03-阶段4执行顺序.md` 进入 Step 12：阶段 4 验收文档。需要补手工验收清单、阶段 4 问题台账收口和阶段 4 验收总结。

## 17. Step 12 复查结论

已完成阶段 4 验收文档收口：

- 新增 `06-阶段4手工验收清单.md`，覆盖启动前检查、Team Profile、工具绑定、Team Chat、fallback、BASIC 回归、Trace Detail、Token Usage 和错误降级。
- 新增 `07-阶段4验收总结.md`，总结阶段 4 已完成能力、提交点、自动化验证、仍暂缓项和完成标准。
- 更新 `05-阶段4问题台账.md` 当前非阻塞项，把已完成的 Team Runtime 接 Chat、Team SSE、Trace Detail Team span 从暂缓列表移除。

Step 12 验证命令：

```powershell
npm --prefix agent-platform-frontend run build
mvn.cmd -s .mvn/settings.xml test
```

结果：上述命令已在 Step 12 收口时通过。前端 build 仍有已知 Vite chunk size warning；Maven 测试中的 `db down` / `alert down` 日志来自故障隔离测试，不代表构建失败。

## 18. 阶段 4 复查修复

重新检查阶段 4 时发现一个配额统计细节问题：Reviewer 第一次要求重试时，第一次 Reviewer 的 `ModelInvokeResult.usage()` 已经会写入 `TokenUsageService.record(...)`，但 `AgentRunResult.usage()` 只汇总了最后一次 Reviewer，导致 Gateway 最终 `quotaFilter.commit(...)` 可能少结算一次 Review token。

已修复：`DefaultTeamRuntimeService` 在 Team run 内保留所有 `TeamReviewResultDTO`，总 usage 汇总时统计所有 Reviewer 调用结果；`DefaultTeamRuntimeServiceTest#retriesReviewerRequestedTaskOnce` 增加回归断言。

复查验证命令：

```powershell
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=DefaultTeamRuntimeServiceTest#retriesReviewerRequestedTaskOnce" test
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core "-Dtest=ArchitectureRulesTest,DefaultAgentRuntimeServiceTest,TeamRunLimiterTest,DefaultTeamRuntimeServiceTest,DefaultTeamPlannerTest,DefaultTeamExecutorTest,DefaultTeamReviewerTest,TeamRuntimeEventDTOTest,NoopTeamEventSinkTest,TaskPlanValidatorTest" test
npm --prefix agent-platform-frontend run test -- team-run-utils parse-sse trace-labels trace-utils
npm --prefix agent-platform-frontend run build
mvn.cmd -s .mvn/settings.xml test
```

结果：上述命令均通过。前端 build 仍有已知 Vite chunk size warning；Maven 测试中的 `db down` / `alert down` 日志来自故障隔离测试，不代表构建失败。

## 19. 下一步

按 `06-阶段4手工验收清单.md` 做浏览器手工验收。手工验收通过后，打阶段标签：

```powershell
git tag phase-4-complete
git push origin main --tags
```

阶段 4 标签完成后，再进入阶段 5 飞书机器人扩展。
