# 阶段 4 执行顺序

> 本文用于阶段 4 实际开发推进。设计边界见：`实际开发/阶段4/01-阶段4AgentTeam设计说明.md`。接口细节见：`实际开发/阶段4/02-阶段4接口设计.md`。

## 1. 总体目标

阶段 4 实现 Agent Team 最小稳定闭环：

```text
Profile executionMode=TEAM
  -> Chat 请求
  -> Planner 生成 TaskPlan
  -> Executor 调用授权 Skill/MCP 或执行 MODEL_TASK
  -> Orchestrator 生成 answerDraft
  -> Reviewer 审查 answerDraft
  -> Orchestrator 汇总最终答案
  -> SSE 阶段性实时展示过程
  -> Trace 展示 Team span
```

阶段 4 不做：

- 新 Team 数据表。
- 工作流设计器。
- 并发执行。
- 复杂 Pipeline 重构。
- 高危工具确认。
- token 级真实流式输出。

## 2. 开发原则

1. 先后端 Team Runtime，后前端展示。
2. 先 mock/规则兜底跑通活动策划 Demo，再接真实模型 JSON 输出。
3. 先定义轻量 AgentTool 统一视图，底层仍复用现有 Skill/MCP 执行接口。
4. Team 数据先落 Trace Span attributes，不新增表。
5. Team 事件必须阶段性实时推送，不做“执行完后一次性刷屏”的假进度。
6. 每个 Step 必须有测试和可验证结果。

## 3. Step 1：Profile executionMode 接口闭环

目标：

- DTO 暴露 `executionMode`。
- 创建和更新 Profile 支持 `BASIC` / `TEAM`。
- 前端 Profile 表单可选择 Basic Agent 或 Team Agent。

后端文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/profile/dto/ProfileDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/profile/command/CreateProfileCommand.java
agent-platform-core/src/main/java/com/ls/agent/core/profile/command/UpdateProfileCommand.java
agent-platform-core/src/main/java/com/ls/agent/core/profile/application/DefaultProfileService.java
agent-platform-web/src/main/java/com/ls/agent/web/dto/CreateProfileRequest.java
agent-platform-web/src/main/java/com/ls/agent/web/dto/UpdateProfileRequest.java
```

前端文件：

```text
agent-platform-frontend/src/features/profiles/CreateProfileDialog.tsx
agent-platform-frontend/src/features/profiles/ProfileDetailPanel.tsx
agent-platform-frontend/src/features/profiles/ProfilesPage.tsx
agent-platform-frontend/src/lib/api/types.ts
```

测试：

```text
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core -am "-Dtest=DefaultProfileServiceTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
mvn.cmd -s .mvn/settings.xml -pl agent-platform-web -am "-Dtest=ProfileControllerTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
npm run build
```

提交建议：

```text
feat(profile): expose agent execution mode
```

## 4. Step 2：新增 core.team 基础 DTO 和校验

目标：

- 建立 Team TaskPlan / ExecutionResult / ReviewResult 类型。
- 实现 TaskPlanValidator。
- 覆盖非法 JSON、重复 taskId、无效 dependsOn、循环依赖、工具不存在、任务数量超限等测试。

新增文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/TeamTaskDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/TaskPlanDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/ExecutionResultDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/ReviewResultDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/TaskPlanValidator.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/TeamLimits.java
agent-platform-core/src/test/java/com/ls/agent/core/team/application/TaskPlanValidatorTest.java
```

校验规则：

```text
goal 不能为空。
tasks 不能为空。
task.id 不能为空且唯一。
task.taskType 只允许 TOOL_TASK / MODEL_TASK。
task.description 不能为空。
TOOL_TASK 必须有 suggestedTool。
MODEL_TASK 不允许有 suggestedTool，避免 Planner 输出歧义。
dependsOn 必须引用存在任务。
dependsOn 不能循环。
TOOL_TASK 的 suggestedTool 必须存在于 AgentTool 列表。
任务数量第一版限制 1-8 个。
```

测试：

```text
mvn.cmd -s .mvn/settings.xml -pl agent-platform-core -am "-Dtest=TaskPlanValidatorTest" "-Dsurefire.failIfNoSpecifiedTests=false" test
```

提交建议：

```text
feat(team): add task plan model and validation
```

## 5. Step 3：新增 AgentTool 统一运行时工具视图

目标：

- 在 `core.agent` 或 `core.team` 内部建立轻量 AgentTool 视图。
- 把 Profile 可用 Skill/MCP 转成统一描述。
- Executor 根据 sourceType 分发到 SkillExecutor / McpToolExecutor。

推荐包：

```text
agent-platform-core/src/main/java/com/ls/agent/core/agent/tool/
```

新增文件：

```text
AgentToolDTO.java
AgentToolSourceType.java
AgentToolResolver.java
DefaultAgentToolResolver.java
AgentToolDispatcher.java
DefaultAgentToolDispatcher.java
```

边界：

- Resolver 可以依赖 `AgentContextDTO`，从 `availableSkills()` 和 `availableMcpTools()` 生成工具列表。
- Dispatcher 继续调用现有 `SkillExecutor` / `McpToolExecutor`。
- 不直接访问 skill/mcp mapper。

测试：

```text
DefaultAgentToolResolverTest
DefaultAgentToolDispatcherTest
ArchitectureRulesTest
```

提交建议：

```text
feat(agent): add unified runtime tool view
```

## 6. Step 4：实现 Planner

目标：

- Planner 根据用户输入、Profile Prompt、AgentTool 列表生成 TaskPlan。
- Planner prompt 中工具列表必须展示 `sourceType + name`，例如 `[SKILL] calculator`、`[MCP] read_file`，即使第一版内部工具名暂不做 namespace。
- 第一版必须有稳定兜底，保证任何 Demo 都不依赖真实模型 JSON 稳定性。

新增文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/team/api/TeamPlanner.java
agent-platform-core/src/main/java/com/ls/agent/core/team/command/PlanTeamCommand.java
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/TeamPlanResultDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/DefaultTeamPlanner.java
```

策略：

```text
1. 优先调用模型生成 TaskPlan JSON。
2. JSON 解析或校验失败时，要求模型修正一次。
3. 仍失败时，生成通用单任务 fallback plan：MODEL_TASK + suggestedTool=null。
4. 上一次错误原因写回 prompt 前必须截断，避免长异常或垃圾输出浪费上下文。
5. `TeamPlanResultDTO.modelInvocations` 必须保留 Planner 每次成功收到的模型响应，供 Step 8 记录 Trace / Token usage。
6. 活动策划三任务 plan 只作为测试 seed 或 Demo Profile 的推荐模板，不写死为 Planner 通用 fallback。
```

测试：

```text
DefaultTeamPlannerTest
```

重点测试：

- 模型返回合法 JSON。
- 模型第一次返回非法 JSON，第二次修正成功。
- 两次都失败时使用通用单任务 fallback plan。
- 模型返回空响应时使用 fallback plan。
- 模型返回超过 maxTasks 的 plan 时触发修正或 fallback。
- fallback plan 不依赖 weather/search/calculator 是否存在。

提交建议：

```text
feat(team): implement planner with fallback plan
```

## 7. Step 5：实现 Executor

目标：

- 按依赖顺序执行任务。
- 对 `TOOL_TASK` 调用 AgentToolDispatcher。
- 对 `MODEL_TASK` 调用 ModelInvokeService，基于用户目标、当前任务、Profile Prompt 和已完成任务结果生成当前任务结果。

新增文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/team/api/TeamExecutor.java
agent-platform-core/src/main/java/com/ls/agent/core/team/command/ExecuteTeamTaskCommand.java
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/TeamTaskExecutionResultDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/DefaultTeamExecutor.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/TaskDependencySorter.java
```

规则：

```text
dependsOn 未成功时，当前任务 skipped。
TOOL_TASK 工具调用成功 -> success。
TOOL_TASK 工具调用失败 -> failed，但不中断整体。
MODEL_TASK 模型调用成功 -> success。
MODEL_TASK 模型调用失败 -> failed。
Executor 不直接写 trace span；它返回 ExecutionResult、ModelInvokeResult、AgentToolDispatchResult。
Step 8 Orchestrator 统一写 task/tool/model trace span，并记录 token usage。
```

MODEL_TASK prompt 必须包含：

```text
1. 用户原始请求。
2. 当前任务 name/description。
3. 已完成任务的 ExecutionResult 摘要。
4. Profile 补充 Prompt。
5. 输出约束：只输出当前任务结果，不重新规划，不生成最终总结。
```

测试：

```text
DefaultTeamExecutorTest
TaskDependencySorterTest
```

重点测试：

- 依赖排序正确。
- 依赖失败时后续任务 skipped。
- TOOL_TASK 调用成功。
- TOOL_TASK 调用失败后返回 failed，Team 不直接崩。
- MODEL_TASK 调用模型成功。
- MODEL_TASK 模型异常后返回 failed。
- maxToolCalls 超限时后续工具任务 failed 或 skipped。

提交建议：

```text
feat(team): execute team tasks with authorized tools
```

## 8. Step 6：实现 Reviewer

目标：

- Reviewer 审查 TaskPlan、ExecutionResult 和 Orchestrator 生成的 answerDraft。
- 输出 ReviewResult。
- 支持失败兜底：如果关键任务成功且至少有一个结果，则可通过但给 warning。

新增文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/team/api/TeamReviewer.java
agent-platform-core/src/main/java/com/ls/agent/core/team/command/ReviewTeamCommand.java
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/TeamReviewResultDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/DefaultTeamReviewer.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/ReviewResultValidator.java
```

规则：

```text
Reviewer 不调用业务工具，不重新规划任务。
Reviewer JSON 非法或校验失败时重试一次。
二次失败时 fallback 为 passed=true + WARN issue，不直接阻断 Team 输出。
TeamReviewResultDTO.modelInvocations 必须保留 Reviewer 模型响应，供 Step 8 记录 Trace / Token usage。
```

测试：

```text
DefaultTeamReviewerTest
ReviewResultValidatorTest
```

重点测试：

- passed=true。
- passed=false 且 retryTasks 有效。
- retryTasks 引用不存在任务时校验失败。
- Reviewer 返回非法 JSON 时 fallback 为 warning review。
- answerDraft 为空时 Reviewer 返回不通过或 warning。

提交建议：

```text
feat(team): add reviewer and review validation
```

## 9. Step 7：实现 Team 实时事件模型

目标：

- 定义 TeamRuntimeEventDTO。
- 定义 TeamEventSink。
- Team Runtime 执行过程中实时 emit。
- Gateway 将事件即时写出到 SSE。

新增文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/team/dto/TeamRuntimeEventDTO.java
agent-platform-core/src/main/java/com/ls/agent/core/team/api/TeamEventSink.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/NoopTeamEventSink.java
```

集成方式：

```text
AgentRunCommand 增加 eventSink，或新增 TeamRunCommand 包含 eventSink。
DefaultTeamRuntimeService 在 plan/task/tool/review/final 节点调用 eventSink.emit(event)。
Core 只调用 TeamEventSink.emit(TeamRuntimeEventDTO)，不感知 OutputStream 或 SSE。
Gateway 提供 TeamEventSink 适配实现，把 TeamRuntimeEventDTO 写成 SSE。
单元测试使用 CapturingTeamEventSink 收集事件顺序。
```

注意：

- 阶段 4 做 Team 事件级实时，不做 token 级流式。
- 如果事件写出失败，应中断 SSE 并返回 error 事件。
- Trace 写入失败不影响 Team 主链路，沿用阶段 2 规则。

测试：

```text
TeamRuntimeEventDTOTest
DefaultTeamRuntimeServiceEventOrderTest
InternalAiControllerTest
```

提交建议：

```text
feat(team): emit team runtime events
```

## 10. Step 8：实现 Team Orchestrator

目标：

- 串起 Planner -> Executor -> answerDraft -> Reviewer -> Retry -> Final。
- 统一保存会话消息和记忆。
- 统一记录 Team trace span。
- 返回 `AgentRunResult`，保证外部接口不变。

新增文件：

```text
agent-platform-core/src/main/java/com/ls/agent/core/team/api/TeamRuntimeService.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/DefaultTeamRuntimeService.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/TeamAnswerDraftBuilder.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/TeamFinalAnswerBuilder.java
agent-platform-core/src/main/java/com/ls/agent/core/team/application/TeamRunLimiter.java
```

流程：

```text
1. emit team_start。
2. Planner 生成 TaskPlan，emit team_plan。
3. Executor 逐任务执行，实时 emit task/tool 事件。
4. TeamAnswerDraftBuilder 生成 answerDraft。
5. Reviewer 审查 answerDraft，emit team_review。
6. 如需重试且未超过 maxRetries，emit team_retry 并重试指定任务。
7. TeamFinalAnswerBuilder 生成最终答案，emit team_final/message/done。
```

TeamRunLimiter 限制：

```text
maxTasks=8
maxRetries=1
maxToolCalls=8
maxModelCalls=6
timeoutMs=120000
```

集成点：

```text
DefaultAgentRuntimeService
AgentContextBuilder
ModelInvokeService
ConversationRepository
MemoryWriteService
TraceService
TokenUsageService
TeamEventSink
```

注意：

- 如果直接让 `DefaultAgentRuntimeService` 委托 Team，要避免循环依赖。
- 共享的保存会话、记录 token、trace span 小方法可以抽成 package-private helper。
- 不为了阶段 4 立刻做完整 Pipeline。

测试：

```text
DefaultTeamRuntimeServiceTest
DefaultAgentRuntimeServiceTest
TeamRunLimiterTest
```

重点测试：

- 正常 Planner -> Executor -> Reviewer -> Final。
- Reviewer 要求重试一次后通过。
- Reviewer 要求重试两次时第二次被限制。
- maxModelCalls 超限。
- timeoutMs 超限。
- 事件顺序符合接口文档。
- BASIC 模式仍走原 Agent Runtime。

提交建议：

```text
feat(team): orchestrate planner executor reviewer runtime
```

## 11. Step 9：Gateway/Web SSE 输出 Team 事件

目标：

- Gateway 在 TeamRuntime 执行过程中实时写出 Team 事件。
- Web 保持 SSE 透传，不缓冲。
- 前端能逐步收到 `team_plan`、`team_task_start`、`team_task_result`、`team_review`。

涉及文件：

```text
agent-platform-gateway/src/main/java/com/ls/agent/gateway/controller/InternalAiController.java
agent-platform-web/src/main/java/com/ls/agent/web/client/HttpGatewayClient.java
agent-platform-web/src/main/java/com/ls/agent/web/controller/ChatController.java
```

测试：

```text
InternalAiControllerTest
ChatControllerTest
前端 parse-sse 测试
```

重点测试：

- Gateway 在 TeamRuntime 未完成前已经写出 `team_plan`。
- Web 转发不合并、不缓冲 Team 事件。
- Team 中途异常时输出 `error` 并结束流。

提交建议：

```text
feat(chat): stream team runtime events
```

## 12. Step 10：前端 Profile 和 Chat Team 面板

目标：

- Profile 可设置 Team 模式。
- Chat 页面展示 Team 事件。
- 右侧 RuntimeDetailPanel 增加 Team Plan / Task / Review / Mermaid。

前端文件：

```text
agent-platform-frontend/src/features/profiles/CreateProfileDialog.tsx
agent-platform-frontend/src/features/profiles/ProfileDetailPanel.tsx
agent-platform-frontend/src/features/profiles/ProfilesPage.tsx
agent-platform-frontend/src/features/chat/ChatPage.tsx
agent-platform-frontend/src/features/chat/RuntimeDetailPanel.tsx
agent-platform-frontend/src/features/chat/team-types.ts
agent-platform-frontend/src/features/chat/TeamRunPanel.tsx
agent-platform-frontend/src/features/chat/team-mermaid.ts
```

视觉规则：

- 不改变阶段 3 已确认的整体布局。
- Team 面板放右侧运行详情中。
- 任务状态用小型状态标记，不做大屏效果。
- Mermaid 只读展示，不做编辑器。
- Mermaid 中 Planner 到任务使用虚线表示“生成计划”，dependsOn 用实线表示执行依赖。

验证：

```text
npm run build
浏览器手工测试 Team Chat
```

提交建议：

```text
feat(frontend): show team run progress in chat
```

## 13. Step 11：Trace Detail 支持 Team Span

目标：

- Trace Timeline 对 Team span 做中文名称映射。
- Span Detail 展示 role、taskId、toolName、retryIndex。
- 长字段避免重叠。

前端文件：

```text
agent-platform-frontend/src/features/traces/TraceTimeline.tsx
agent-platform-frontend/src/features/traces/SpanDetailPanel.tsx
agent-platform-frontend/src/features/traces/trace-labels.ts
```

验证：

```text
npm run build
打开 Team Chat 产生的 Trace Detail
```

提交建议：

```text
feat(frontend): render team spans in trace detail
```

## 14. Step 12：阶段 4 验收文档

目标：

- 生成手工验收清单。
- 更新阶段 4 问题台账。
- 生成阶段 4 验收总结。

新增 / 更新文件：

```text
实际开发/阶段4/06-阶段4手工验收清单.md
实际开发/阶段4/05-阶段4问题台账.md
实际开发/阶段4/07-阶段4验收总结.md
```

验收命令：

```text
mvn.cmd -s .mvn/settings.xml test
npm run build
```

提交建议：

```text
docs(stage4): add team acceptance documents
```

## 15. 阶段完成标准

满足以下条件即可认为阶段 4 完成：

- Team Profile 可以创建或更新。
- Team Chat 能跑通活动策划 Demo。
- SSE 能实时展示 Team 过程，而不是结束后一次性刷屏。
- Trace Detail 能看到 Team spans。
- Token Usage 不因 Team 模式断链。
- 基础 Agent BASIC 模式不回归。
- Team 上限和超时有测试覆盖。
- 后端全量测试通过。
- 前端 build 通过。
- 手工验收清单完成。

阶段 4 完成后：

```text
git tag phase-4-complete
git push origin main --tags
```

## 16. Step 3 补充约定：AgentTool 统一工具视图

Step 3 最终采用以下设计，作为后续编码依据：

```text
包位置：
  agent-platform-core/src/main/java/com/ls/agent/core/agent/tool/

核心类：
  AgentToolDTO
  AgentToolSourceType
  AgentToolRiskLevel
  AgentToolResolver
  DefaultAgentToolResolver
  AgentToolDispatchCommand
  AgentToolDispatchResult
  AgentToolDispatcher
  DefaultAgentToolDispatcher
```

接口契约：

```text
AgentToolResolver.resolve(AgentContextDTO context) -> List<AgentToolDTO>
AgentToolDispatcher.dispatch(AgentToolDispatchCommand command) -> AgentToolDispatchResult
```

字段约定：

```text
name            稳定工具名；Skill 使用 SkillDTO.code，MCP 使用 McpToolDTO.name
displayName     展示名；Skill/MCP 第一版都使用 DTO.name
description     工具描述
sourceType      SKILL / MCP
parameterSchema 参数 schema
riskLevel       第一版统一 LOW，后续接安全策略/高危工具确认时再计算
```

设计取舍：

- AgentTool 放 `core.agent.tool`，不放 `core.team`，因为它是 Agent Runtime 通用工具视图。
- Resolver 只依赖 `AgentContextDTO`，从 `availableSkills()` 和 `availableMcpTools()` 生成工具列表。
- Resolver 不调用 `SkillQueryService` / `McpToolQueryService`，避免和 AgentContextBuilder 重复查库。
- Planner、Validator、Executor 使用同一份 AgentTool 列表，避免 Planner 规划出 Executor 不可用的工具。
- Dispatcher 继续调用现有 `SkillExecutor` / `McpToolExecutor`。
- 不直接访问 skill/mcp mapper。
- 第一版不做 namespaced name；如果后续出现 Skill/MCP 重名，再升级为 `skill:xxx` / `mcp:xxx`。
