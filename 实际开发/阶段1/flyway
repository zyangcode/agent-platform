❯  这玩意帮你在项目启动打包时，帮你将所有的sql执行                                                                                                                                                    

● 对，准确说不是"打包时"，是 Spring Boot 启动时。

  mvn package        ← 打包，只是编译成 jar，不执行 SQL

  java -jar xxx.jar  ← 启动应用，这时 Flyway 才干活：
    │
    ├─ 检查 flyway_schema_history 表，知道哪些 SQL 已经执行过
    ├─ 找到 db/migration/ 下还没执行过的新 SQL 文件
    ├─ 按版本号顺序执行
    └─ 执行完把版本号写入 flyway_schema_history

  这样做的好处：每次部署重启，数据库的 Schema 自动更新到最新版本，不用你手动跑 SQL。多人协作时也不会出现"我本地少了某个字段，因为忘了跑那条 ALTER TABLE"。