# 阶段 1 执行顺序

> 本文档用于真实开发时按步骤推进。设计依据：`考核设计/功能设计/03-MVP分阶段实现路线图.md`、`考核设计/架构与模块设计/04-模块目录与包结构设计.md`、`考核设计/接口设计/01-阶段1接口设计.md`、`考核设计/开发检查清单/01-阶段1开发检查清单.md`。

---

## 1. 阶段目标

阶段 1 只完成基础 Agent 闭环：

```text
用户注册登录
创建 Application / API Key
配置模型
创建 Profile
绑定 Skill / MCP
Web -> Gateway -> Core 发起对话
Agent 调模型和工具
SSE 实时返回
保存会话消息
写入长期记忆
```

阶段 1 不做完整治理：

```text
完整 Trace 查询
完整 Token 配额预扣
完整脱敏策略
完整飞书告警
完整 OpenAI 兼容接口
前端控制台
多 Agent Team
```

当前开发进度：

```text
已完成到 Step 15：阶段 1 总体验收。
最近一次全量测试 79 个通过。
Web -> Gateway -> Core 的完整 Chat SSE 链路已跑通。
阶段 1 手工测试步骤见：实际开发/阶段1/03-阶段1测试指南.md。
```

---

## 2. 开发前准备

确认软件：

```text
JDK 17+
Maven 3.9+
PostgreSQL 15/16
Docker Desktop
IDEA
Git
Apifox
DBeaver
curl.exe
```

命令检查：

```powershell
java -version
mvn -version
curl.exe --version
```

数据库准备：

```text
创建本地 PostgreSQL 数据库。
准备 application-local.yml。
确认 DBeaver 能连接数据库。
```

Git 提交与标签约定：

```text
Step 级别进度只写清楚 commit message，不再为每个 Step 打 tag。
阶段 1 全部完成并通过验收后，再打 phase-1-complete。
MVP 可演示版本完成后，再打 mvp-demo。
已存在的 phase-1、phase-1-step-11 标签保留，不再调整。
```

---

## 3. 执行步骤

### Step 1：创建 Maven 多模块工程

创建：

```text
agent-platform-common
agent-platform-core
agent-platform-web
agent-platform-gateway
```

父 POM 约束：

```text
父 POM 使用 dependencyManagement / pluginManagement 统一管理依赖版本和插件版本。
业务依赖放到各子模块 dependencies，避免父 POM dependencies 污染所有模块。
Spring Boot BOM / Spring AI BOM 在父 POM dependencyManagement 中导入。
Spring Boot 使用 3.4.x 版本线，保证与 Spring AI 1.0.x 兼容。
```

验收：

```text
mvn test 能跑通。
frontend 不加入 Maven modules。
feishu-bot 不依赖 agent-platform 代码。
```

### Step 2：定义 common 基础能力

实现：

```text
ApiResponse
PageResult
ErrorCode
BizException
IdGenerator / TimeUtils 可选
```

禁止：

```text
common 放业务 DTO
common 放 Entity
common 放 Mapper
```

验收：

```text
common 不依赖 core/web/gateway。
```

### Step 3：编写阶段 1 Flyway SQL

脚本位置：

```text
agent-platform-core/src/main/resources/db/migration/
```

阶段 1 必建表：

```text
tenants
users
roles
user_roles
applications
api_keys
model_providers
model_configs
agent_profiles
skills
skill_versions
profile_skills
mcp_servers
mcp_tools
profile_mcp_tools
conversations
conversation_messages
memories
```

初始化数据：

```text
默认 tenant
ADMIN / DEVELOPER / USER 角色
默认 Admin 用户
内置 Skill：calculator/weather/search
至少 1 个 MCP 示例工具
```

验收：

```text
应用启动后 Flyway 成功执行。
DBeaver 能看到阶段 1 表。
api_keys 只保存 hash，不保存完整明文。
```

### Step 4：生成 Entity / Mapper

按表归属放置：

```text
core.identity.entity / mapper
core.model.entity / mapper
core.profile.entity / mapper
core.skill.entity / mapper
core.mcp.entity / mapper
core.agent.entity / mapper
core.memory.entity / mapper
```

验收：

```text
web/gateway 没有 Mapper。
common 没有 Entity。
Entity 字段与数据库模型一致。
```

### Step 5：加入 ArchUnit

最低规则：

```text
web/gateway 禁止访问 core.*.mapper
core 内部跨业务包禁止访问其他包 entity/mapper/internal
Controller 禁止直接访问 Mapper
common 禁止包含业务 DTO/Entity/Mapper/Service
```

验收：

```powershell
mvn test
```

必须包含 ArchUnit 测试。

### Step 6：实现 identity

实现接口：

```text
POST /api/auth/register
POST /api/auth/login
GET /api/auth/me
POST /api/applications
GET /api/applications
GET /api/applications/{applicationId}/api-keys
POST /api/applications/{applicationId}/api-keys/{apiKeyId}/revoke
```

Core 能力：

```text
AuthService
ApplicationService
```

说明：

```text
Step 6 当前已落地 AuthService / ApplicationService。
API Key 创建、查询、吊销暂由 ApplicationService 承接。
Gateway API Key 校验进入 Step 13 前再新增 ApiKeyService。
PermissionService 暂不单独拆，资源归属校验先放各业务 Application Service。
```

Web 安全能力：

```text
SecurityConfig
JwtAuthenticationFilter
JwtTokenService
JwtProperties
CurrentUser
RestAuthenticationEntryPoint
RestAccessDeniedHandler
PasswordEncoder / BCryptPasswordEncoder
```

验收：

```text
Apifox 能注册、登录、创建 Application。
登录后返回 JWT。
GET /api/auth/me 能通过 Authorization: Bearer <jwt> 返回当前用户。
未登录访问受保护接口返回 401 JSON。
USER 访问 /api/admin/** 返回 403 JSON。
创建 Application 时返回一次 API Key 明文。
DBeaver 中 api_keys 只有 hash 和 prefix。
```

### Step 7：实现 model

实现：

```text
POST /api/admin/model-providers
POST /api/admin/model-configs
GET /api/model-configs
core.model.api.ModelInvokeService
```

要求：

```text
Spring AI / OpenAI 兼容 Client 只封装在 core.model。
不让 Spring AI 接管 Agent Runtime。
Spring AI 的 Flux / ChatClient / Tool Calling 细节不向 core.agent / gateway / web 暴露。
模型 API Key 加密存储。
```

验收：

```text
Admin 能创建模型供应商和模型配置。
ModelInvokeService 能完成一次简单文本调用。
```

### Step 8：实现 profile

实现：

```text
POST /api/profiles
GET /api/profiles
GET /api/profiles/{profileId}
PUT /api/profiles/{profileId}/skills
PUT /api/profiles/{profileId}/mcp-tools
```

验收：

```text
Profile 可创建。
DRAFT Profile 创建者可测试。
Skill/MCP 绑定表有记录。
Core application 层校验访问权限。
```

### Step 9：实现 skill

实现内置 Skill：

```text
calculator：真实实现
weather：可 mock
search：可 mock
```

Core 能力：

```text
SkillRegistry
SkillExecutor
SkillQueryService
```

验收：

```text
GET /api/skills 返回 3 个内置 Skill。
calculator 能执行 128 * 36 + 59。
agent 不能直接访问 skill.mapper。
```

### Step 10：实现 mcp

实现至少 1 个 MCP Client 示例工具：

```text
filesystem 只读工具
或 readonly-sql 查询工具
```

Core 能力：

```text
McpToolRegistry
McpToolExecutor
McpToolQueryService
```

验收：

```text
GET /api/mcp-tools 返回至少 1 个 AVAILABLE 工具。
Agent 后续能调用该工具。
```

### Step 11：实现 context

实现：

```text
core.context.api.AgentContextBuilder
```

最小组装：

```text
System Prompt
Profile Prompt
用户输入
短期历史消息
长期记忆召回结果
可用 Skill 列表
可用 MCP Tool 列表
```

验收：

```text
AgentContextBuilder 单测通过。
上下文组装不放 Gateway。
阶段 1 不强制做复杂压缩，但 token 预算和简单裁剪位置固定在 core.context。
后续摘要压缩仍由 core.context 承接。
```

### Step 12：实现 agent

实现：

```text
core.agent.api.AgentRuntimeService
ConversationRepository
MessageHistoryService
基础 ReAct while 循环
```

要求：

```text
调用模型只通过 core.model.api.ModelInvokeService。
调用 Skill 只通过 core.skill.api.SkillExecutor。
调用 MCP 只通过 core.mcp.api.McpToolExecutor。
保存 conversation_messages。
写入 memories。
```

验收：

```text
Agent 能完成普通对话。
Agent 能调用 calculator。
Agent 能调用 MCP 示例工具。
conversation_messages 有记录。
memories 有记录。
```

当前落地：

```text
已实现 core.agent.api.AgentRuntimeService。
已实现 AgentRunCommand / AgentRunResult。
已实现 DefaultAgentRuntimeService 最小 Agent 循环：
- 调 AgentContextBuilder 组装上下文。
- 调 ModelInvokeService 生成回复。
- 支持 @skill:name {...} 调用 SkillExecutor。
- 支持 @mcp:name {...} 调用 McpToolExecutor。
- 保存 user / assistant conversation_messages。
- 通过 MemoryWriteService 写入 SUMMARY memory。
已修复 MCP Tool 绑定校验必须限定当前 tenant 的 active MCP Server。
```

### Step 13：实现 gateway 空壳

实现：

```text
GET /internal/ai/stream-test
POST /internal/ai/chat/stream
POST /api/ai/chat/stream
```

要求：

```text
校验 X-Internal-Token。
API Key 调 core.identity.api.ApiKeyService 校验；如果 Step 6 尚未拆出该接口，本步骤先补齐。
生成简单 traceId。
GatewayContext finally 清理。
显式转换 AgentRunCommand / ModelInvokeCommand。
agentMode=agent 调 core.agent.api.AgentRuntimeService；agentMode 为空时默认 agent。
agentMode=none 调 core.model.api.ModelInvokeService。
不访问 core.*.mapper。
```

验收：

```powershell
curl.exe -N -H "X-Internal-Token: dev-internal-token" http://localhost:8081/internal/ai/stream-test
```

终端逐条输出 SSE 事件。

当前落地：

```text
已实现 gateway.controller.InternalAiController。
已实现 GET /internal/ai/stream-test 固定 SSE 测试接口。
已实现 POST /internal/ai/chat/stream，内部 Web 请求通过 X-Internal-Token 鉴权后转 AgentRuntimeService。
已实现 POST /api/ai/chat/stream，外部 API Key 请求通过 ApiKeyService 鉴权后转 AgentRuntimeService。
已实现 agentMode=none 的纯模型转发入口。
已实现 GatewayExceptionHandler 返回统一 JSON 错误。
已补 core.identity.api.ApiKeyService / ApiKeyAuthResult / DefaultApiKeyService。
SSE 使用 Spring MVC StreamingResponseBody，每个事件写入后 flush。
MockMvc 测试 SSE 时需要 asyncDispatch。
当前 Step 13 相关测试通过，全量 mvn.cmd -s .mvn/settings.xml test 共 73 个测试通过。
```

### Step 14：实现 web 最小接口与 SSE 透传

实现：

```text
补齐并联调 Web Auth / Application / Model / Profile / Skill / MCP / Conversation Controller
GET /api/chat/stream-test
POST /api/chat/stream
```

要求：

```text
前面已实现的 Web Controller 在本步骤统一联调。
Web AI 请求不能直接调 core.agent。
Web 通过 HTTP 调 Gateway。
Web 逐事件透传 Gateway SSE。
Gateway 出错时 Web 转成 SSE error 事件。
```

验收：

```powershell
curl.exe -N -H "Authorization: Bearer <jwt>" http://localhost:8080/api/chat/stream-test
```

终端逐条输出事件。

当前落地：

```text
已实现 web.controller.ChatController。
已实现 GET /api/chat/stream-test，JWT 鉴权后调用 Gateway /internal/ai/stream-test。
已实现 POST /api/chat/stream，JWT 鉴权后从 CurrentUser 注入 tenantId/userId，再转发到 Gateway /internal/ai/chat/stream。
已实现 web.client.GatewayClient / HttpGatewayClient。
Web 不依赖 gateway Maven 模块，不复用 gateway.dto，HTTP 边界内使用 web.client.InternalChatStreamRequest。
HttpGatewayClient 使用 JDK HttpClient 连接 Gateway，边读边写边 flush，避免 SSE 被 Web 重新缓冲。
SecurityConfig 已放行 DispatcherType.ASYNC，避免 Spring MVC 异步 SSE dispatch 被安全链二次拦截。
Gateway 转发异常时，Web 会在 SSE 流内写出 event: error，而不是交给普通 JSON 全局异常处理器。
Gateway 自身流内异常会写出 event: error，避免直接调用 Gateway 时拿不到 SSE 错误事件。
HttpGatewayClient 已设置 3 秒连接超时，避免 Gateway 未启动时 Web 请求长时间卡住。
当前 Step 14 相关测试通过，全量 mvn.cmd -s .mvn/settings.xml test 共 78 个测试通过。
```

### Step 15：阶段 1 总体验收

用 Apifox 验证：

```text
注册
登录
创建 Application
创建模型供应商
创建模型配置
创建 Profile
绑定 Skill
绑定 MCP Tool
发起对话
查询会话
查询消息
```

用 curl 验证：

```text
Gateway 固定 SSE
Web 固定 SSE 透传
真实 Agent 对话 SSE
```

用 DBeaver 验证：

```text
users
applications
api_keys
agent_profiles
profile_skills
profile_mcp_tools
conversations
conversation_messages
memories
```

最终完成标准：

```text
Postman/Apifox 能通过 Web -> Gateway -> Core 路径跑通 Agent 对话。
SSE 是实时返回，不是一次性返回。
calculator Skill 可调用。
至少 1 个 MCP 示例工具可调用。
历史消息可查询。
长期记忆有持久化记录。
ArchUnit 纳入 mvn test。
```

当前落地：

```text
已完成 Step 15 总体验收。
已修复真实启动时 core Mapper 未扫描的问题：MybatisPlusConfiguration 从 @ConditionalOnBean(DataSource.class) 改为按 spring.datasource.url 启用。
已新增 MybatisPlusConfigurationTest，验证有数据源配置时 core Mapper 能注册。
已使用 PostgreSQL 16 + Gateway 8081 + Web 8080 跑通真实链路。
Gateway 固定 SSE：GET /internal/ai/stream-test 返回 thinking/action/observation/message/done。
Web 固定 SSE 透传：GET /api/chat/stream-test 返回 thinking/action/observation/message/done。
完整 Chat SSE：登录 admin -> 创建 Application -> 创建 Profile -> POST /api/chat/stream，返回 thinking/message/done。
当前完整 Chat SSE 最终返回 mock 模型消息：[mock-chat] Echo: hello。
当前 ReAct 中间步骤还不会实时逐步推送到 Web SSE；阶段 1 只保证 request accepted、最终 message、done 和 error 事件稳定返回。
ReAct 过程事件实时化放阶段 2/后续增强，通过 AgentRuntime listener / event callback 机制解决。
最近一次全量测试命令：mvn.cmd -s .mvn/settings.xml test。
最近一次全量测试结果：79 tests，BUILD SUCCESS。
```

---

## 4. 阻塞判断

遇到阻塞时按下面处理：

```text
模型不可用：
  先用 mock model，不能阻塞主链路。

天气 / 搜索外部 API 不可用：
  使用 mock Skill 返回。

MCP 工具接入复杂：
  先做只读本地示例工具。

SSE 被缓冲：
  先停下修 Web -> Gateway 透传，不继续写 Agent。

ArchUnit 拦截越界：
  优先调整包依赖，不绕过测试。
```

---

## 5. 每步完成后必须回查

每完成一个步骤，回查：

```text
考核设计/开发检查清单/01-阶段1开发检查清单.md
```

不要等阶段 1 全写完再查。
